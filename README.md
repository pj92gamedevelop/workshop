git log to see files and file versions

git restore --source <commit-hash> filename (to restore to old committed version)

git restore can also be used to unstage a file

git restore can also be used to undo changes made in the working directory

ls -a    (to list all files in your repo folder)

git add to stage

git commit to send it to local repo

git status to see all tracked and untracked files

git commit -am (commit immediately without adding, doesn’t work with new files).

git push -u origin main (pushing from main branch to github)

git remote -v (check that remote exist)

git remote add origin to link local repo with github repo

git clone and url to clone repository
